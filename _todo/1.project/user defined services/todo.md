Let users define their own services for NPCs or tools.
It is because of things like variables and events.

let them have a folder called "modules" and in that a folder put go files that inherit the service functions. htey can then be added to the game config.

Todo
- user defines game config
- user defines services
