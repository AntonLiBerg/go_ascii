# Agent Notes

- This folder is part of the Go module `go_ascii`.
- Keep this package focused on pure helper methods 
- Prefer deterministic methods with no I/O, no hidden global state, and no mutation of receiver state.
- The package name is `aiapi`; callers can import it from `go_ascii/aiAPI`.
